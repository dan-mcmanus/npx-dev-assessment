import { CalculateTaxPipe } from './calculate-tax.pipe';

describe('CalculateTaxPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateTaxPipe();
    expect(pipe).toBeTruthy();
  });
});
